name: Dev Deploy & Test

on:
  push:
    branches:
      - dev

permissions:
  contents: read
  security-events: write

env:
  AWS_REGION: 'ap-northeast-1'

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment: dev

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 18
          cache: npm

      - run: npm ci

      - uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Deploy
        run: |
          # CDK Bootstrap強制更新
          npm run cdk bootstrap aws://339712731337/ap-northeast-1 --force
          
          # CDKアセットクリア
          rm -rf cdk.out
          
          # 再デプロイ
          npm run cdk deploy -- --all --context envname=dev --require-approval never --verbose
        
      - name: Wait for CloudFront cache invalidation
        run: |
          echo "Waiting for CloudFront cache invalidation..."
          sleep 60
          
          DISTRIBUTION_ID=$(aws cloudformation describe-stacks \
            --stack-name AiIssueManager-dev \
            --query 'Stacks[0].Outputs[?OutputKey==`CloudFrontDistributionId`].OutputValue' \
            --output text)
          
          if [ ! -z "$DISTRIBUTION_ID" ]; then
            echo "CloudFront Distribution ID: $DISTRIBUTION_ID"
            aws cloudfront create-invalidation \
              --distribution-id "$DISTRIBUTION_ID" \
              --paths "/*" || echo "Manual invalidation failed, but deployment continues"
          fi
        
      - name: Health check
        run: |
          sleep 30
          API_URL=$(aws cloudformation describe-stacks \
            --stack-name AiIssueManager-dev \
            --query 'Stacks[0].Outputs[?OutputKey==`ApiUrl`].OutputValue' \
            --output text)
          if [ ! -z "$API_URL" ]; then
            curl -f "${API_URL}projects" || echo "API endpoint not available"
          fi

  integration-test:
    needs: deploy
    runs-on: ubuntu-latest
    environment: dev

    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 18
          cache: npm
      - run: npm ci
      - run: npm install --save-dev axios
      - uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ env.AWS_REGION }}
      - run: |
          API_URL=$(aws cloudformation describe-stacks \
            --stack-name AiIssueManager-dev \
            --query 'Stacks[0].Outputs[?OutputKey==`ApiUrl`].OutputValue' \
            --output text)
          export API_URL
          if [ -f "tests/integration.test.js" ]; then
            node tests/integration.test.js
          else
            echo "Integration test file not found, skipping..."
            mkdir -p test-results/integration
            echo '{"status":"skipped","reason":"test file not found"}' > test-results/integration/result.json
          fi
      - name: Upload integration results
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: integration-test-results
          path: test-results/integration/

  e2e-test:
    needs: deploy
    runs-on: ubuntu-latest
    environment: dev

    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 18
          cache: npm
      - run: npm ci
      - run: npm install --save-dev playwright @playwright/test
      - run: npx playwright install chromium
      - uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ env.AWS_REGION }}
      - run: |
          FRONTEND_URL=$(aws cloudformation describe-stacks \
            --stack-name AiIssueManager-dev \
            --query 'Stacks[0].Outputs[?OutputKey==`WebsiteUrl`].OutputValue' \
            --output text)
          if [ -f "playwright.config.js" ]; then
            sed -i "s|http://localhost:8080|${FRONTEND_URL}|g" playwright.config.js
            npx playwright test
          else
            echo "Playwright config not found, skipping E2E tests..."
            mkdir -p test-results screenshots playwright-report
            echo '{"status":"skipped","reason":"playwright config not found"}' > test-results/e2e-result.json
          fi
      - name: Upload E2E results
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: e2e-test-results
          path: |
            test-results/
            screenshots/
            playwright-report/

  # Generate comprehensive test report
  generate-report:
    name: 'Generate Test Report'
    needs: [integration-test, e2e-test]
    runs-on: ubuntu-latest
    if: always()
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: 18
        cache: npm
        
    - name: Install dependencies
      run: |
        npm ci
        npm install --save-dev exceljs
        
    - name: Download all test artifacts
      uses: actions/download-artifact@v4
      
    - name: Generate Excel report
      run: |
        ls -la scripts/
        node scripts/generate-comprehensive-report.js
      
    - name: Upload final report
      uses: actions/upload-artifact@v4
      with:
        name: comprehensive-test-report
        path: |
          test-report.xlsx
          test-summary.json
